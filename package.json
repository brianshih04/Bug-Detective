{
  "name": "bug-detective",
  "version": "2.5.0",
  "private": true,
  "description": "RAG-based Root Cause Analysis tool for C/C++ codebases (FastAPI + Python backend)",
  "scripts": {
    "dev": "python3 -m uvicorn backend.server:app --host 0.0.0.0 --port 17580 --reload",
    "start": "python3 -m uvicorn backend.server:app --host 0.0.0.0 --port 17580",
    "test": "python3 -m pytest tests/ -v",
    "ingest": "python3 scripts/ingest.py"
  }
}
